apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webapi-core.fullname" . }}-test-connection"
  labels:
    {{- include "webapi-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webapi-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
